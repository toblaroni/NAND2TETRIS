class Main {
    function void main() {
        var GameOfLife gol; 

        let gol = GameOfLife.new(5);
        do gol.draw();

        while (true) {
            do gol.update();
            do gol.draw();
            do Sys.wait(500);
        }

        return;
    }
}