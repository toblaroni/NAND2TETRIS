class Main {
    function void main() {
        var GameOfLife gol; 
        var int iterations, i;

        let gol = GameOfLife.new(8);
        do gol.draw();

        while (true) {
            do gol.update();
            do gol.draw();
            do Sys.wait(500);

            let i = i + 1;
        }

        return;
    }
}