class Main {
    function void main() {
        var GameOfLife gol; 

        let gol = GameOfLife.new(30);

        while (true) {
            do gol.draw();
            do gol.update();
            do Sys.wait(500);
        }

        return;
    }
}