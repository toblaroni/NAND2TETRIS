class Main {
    function void main() {
        var GameOfLife gol; 

        let gol = GameOfLife.new(8);
        do gol.draw_board();

        while (true) {
            do gol.update();
            do gol.draw_board();
            do Sys.wait(500);
        }

        return;
    }
}